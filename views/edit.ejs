<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-zinc-900 text-white w-full min-h-screen">
        <div class="pt-2 flex justify-end pr-5">
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        <h1 class="text-2xl ml-12 mt-2">Edit a Post.</h1>
        <form action="/update/<%= post._id %>" method="post" class=" ml-12">
            <textarea name="content" placeholder="What's in you Mind..?"
                class="w-1/3 p-3 mt-4 block resize-none bg-transparent border-2 border-zinc-800 outline-none"><%= post.content  %></textarea>
            <input class="bg-yellow-500 text-black block rounded-lg px-4 py-2 mt-4" type="submit" value="Submit">
        </form>
    </div>

</body>

</html>