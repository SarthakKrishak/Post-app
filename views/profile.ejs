<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-zinc-900 text-white w-full min-h-screen">
        <div class="pt-2 flex justify-end pr-10">
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        <h1 class="text-3xl pt-1 pl-10">Welcome <%= person.username %>👋</h1>
        <h1 class="text-2xl ml-10 mt-10">Create a Post.</h1>
        <form action="/post" method="post" class=" ml-12">
            <textarea name="content" placeholder="What's in you Mind..?"
                class="w-1/3 p-3 mt-4 block resize-none bg-transparent border-2 border-zinc-800 outline-none"></textarea>
            <input class="bg-blue-500 block rounded-lg px-4 py-2 mt-4" type="submit" value="Create">
        </form>

        <div class="posts ml-10 mt-20">
            <h1 class="text-xl mb-3">Your Posts.</h1>
            <div class="postcontainer flex flex-wrap">
                <% person.post.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mb-2 p-4 ml-1 rounded-md border-2 border-zinc-700 bg-zinc-800">
                        <h2 class="text-blue-500">@<%= person.username %>
                        </h2>
                        <h4 class="tracking-tight text-sm">
                            <%= post.content %>
                        </h4>
                        <h3 class="text-sm mt-3">
                            <%= post.date %>
                        </h3>
                        <h5 class="text-sm text-zinc-400 mt-2">
                            <%= post.likes.length %> likes
                        </h5>
                        <div class="btns flex mt-2">

                            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mr-1"
                                href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(person._id) === -1? "Like":"Unlike"  %>
                            </a>
                            <a class="bg-zinc-500 rounded-md px-3 py-2 text-sm" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                    <% }) %>
            </div>

        </div>
    </div>
    </div>

</body>

</html>